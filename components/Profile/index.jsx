import React from "react";
import styled from "styled-components";

export const Profile = () => {
  return <ProfileContainer> Profile Info </ProfileContainer>;
};

const ProfileContainer = styled.div`
  height: 100%;
  width: 100%;
`;
